import { FormFieldProps } from "./FormFields";

type FormDropdownFieldProps = Omit<FormFieldProps, "field_type">;

function FormDropdownField(props: FormDropdownFieldProps) {
  const {} = props;
  return <></>;
}

export default FormDropdownField;
